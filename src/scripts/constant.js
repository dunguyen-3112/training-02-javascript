const API_BASE = "http://localhost:3000";

const $ = document.querySelector.bind(document),
    rootSelector = "#root";

export {
    API_BASE,
    $,
    employeeSelector,
    loginSelector,
    rootSelector,
    employeeTodoSelector,
};
